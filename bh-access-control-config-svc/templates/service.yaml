apiVersion: v1
kind: Service
metadata:
  name: {{ include "bh-access-control-config-svc.fullname" . }}
  labels:
    {{- include "bh-access-control-config-svc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bh-access-control-config-svc.selectorLabels" . | nindent 4 }}
